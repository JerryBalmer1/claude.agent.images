# Skills

Skills referenced by plans but not yet built live here as placeholders.

When a plan lists a skill in `skills_to_build`, it should appear here (even as a stub) so `Skills.Audit` can track it.

Do not silently skip missing skills. List them. Build them. Or admit they're missing.
